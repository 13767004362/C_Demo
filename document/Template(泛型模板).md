前言：

> 在c++中，是通过模板来实现泛型功能，可用于函数和类中。



模板函数：

```c++
template <typename type> ret-type func-name(parameter list)
{
   // 函数的主体
}
```

模板类：

```c++
template <class type1,class type2> class class-name {
.
.
.
}
```

